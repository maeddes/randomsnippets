helm ls
helm ls --tls
helm init
helm ls --tls --debug
helm install stable/wordpress --debug
helm install stable/wordpress
helm ls --debug
helm init --service-account tiller --upgrade
ls /Users/matthiashaeussler/.helm
helm version
helm home
history | grep helm
helm history
helm install install/kubernetes/helm/istio --name istio --namespace istio-system
helm install install/kubernetes/helm/istio-init --name istio-init --namespace istio-system
helm dep update install/kubernetes/helm/istio
helm repo update
helm --upgrade
helm init --service-account tiller
helm list
kubectl apply -f install/kubernetes/helm/helm-service-account.yaml
helm template install/kubernetes/helm/istio-init --name istio-init --namespace istio-system
helm repo list
helm repo add istio.io https://storage.googleapis.com/istio-release/releases/1.3.1/charts/
helm
helm template install/kubernetes/helm/istio-init --name istio-init --namespace istio-system | kubectl apply -f -
helm install
helm delete esteemed-goat
helm status
helm delete heapster
helm delete stable/heapster
helm uninstall stable/heapster
helm install stable/heapster

helm install stable/mysql
helm install stable/mysql --debug
helm ls
helm ls --debug
helm
helm version
helm version --debug
helm init
helm reset
helm init --debug
helm list
helm home
helm fetch   --repo https://kubernetes-charts.storage.googleapis.com   --untar   --untardir ./charts   --version 5.5.3     prometheus
helm template vendor/cf-operator/deploy/helm/cf-operator        --set "image.tag=${CFOPERATOR_TAG}"       --set "provider=gke" | kubectl apply -f -
export PATH=$PWD/bin:$PWD/vendor/helm-tiller-manager/bin:$PATH
helm
helm
helm
helm version
ls ~/.helm/
./add_helm.sh
helm install stable/wordpress
helm
helm list
helm delete jazzy-quokka
helm ls
helm template
echo $(helm home)
sed -i -e 's/create/delete/g' remove_secure_helm.sh
helm version
ls -ltrR ~/.helm
brew uninstall kubernetes-helm
helm init
curl -L https://git.io/get_helm.sh | bash
helm install stable/wordpress
helm install stable/wordpress --debug
history | grep helm
history | grep helm | cut -c 8-

brew install kubernetes-helm
brew uninstall kubernetes-helm
brew upgrade kubernetes-helm
brew update kubernetes-helm
brew upgrade kubernetes-helm
brew cask install kubernetes-helm
